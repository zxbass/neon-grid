# 184 — TCP-рукопожатие

> Уровень: `★★★★☆` · Тема: TCP · рукопожатие

TCP-рукопожатие: три шага, чтобы начать разговор. Джез впервые видит, как в Сетке здороваются — с
числами вместо имён.
— Рукопожатие — это вежливость, — Зен говорит это ровно. — Но вежливость в Сетке — тоже протокол.
Джез учится вежливости.

---

## Данные

- `data/184/tcp.txt` — сегменты hex, по одному на строку

## Часть 1

Верни:

```text
SYN SEQ=1000
SYN-ACK SEQ=5000 ACK=1001
ACK SEQ=1001 ACK=5001
ESTABLISHED
```

## Часть 2

Верни `DATA: 14 bytes`, `PAYLOAD: GET / HTTP/1.0` и `CLOSE: FIN-FIN-ACK-ACK` (три строки).
