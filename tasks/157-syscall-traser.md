# 157 — Syscall-трассировщик

> Уровень: `★★★★☆` · Тема: syscall · трассировка

Syscall-трассировка: каждый системный вызов — это след. Джез учится читать следы, которые программы оставляют в ядре.
— Следы не врут, — Прах смотрит на лог. — Они просто не знают, что за ними следят.
Джез следит.

---

## Данные
- `data/157/trace.txt`

## Часть 1
Верни по строке на вызов (алфавитный порядок имён): `имя x<количество>`.

## Часть 2
Верни `TOTAL: <всего вызовов>` и `FAILED: <вызовов с -1>`.
