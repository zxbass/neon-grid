# 152 — ELF-загрузчик

> Уровень: `★★★★☆` · Тема: ELF · загрузчики

ELF-файл, разобранный на кости: заголовки, сегменты, секции. Джез учится читать исполняемый файл,
как конструктор — от рамы до болтов.
— Загрузчик не гадает, — Прах смотрит на заголовок. — Он читает. Прочитай и ты.
Джез читает по-загрузочному.

---

## Данные

- `data/152/prog.elf`

## Часть 1

Верни `CLASS: ELF64 TYPE: EXEC MACHINE: x86-64 ENTRY: 0x401000`.

## Часть 2

Верни `SECTIONS: 2 (.text, .shstrtab)` и `TEXT SIZE: 16` (размер `.text`).
