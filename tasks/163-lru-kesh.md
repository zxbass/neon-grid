# 163 — LRU-кэш

> Уровень: `★★★★★` · Тема: LRU · кэши

LRU-кэш в терминале Дона: то, что использовали недавно, — под рукой. Джез учится помнить нужное и забывать лишнее вовремя.
— Память — это выбор, — Дон ставит перед Джез кружку. — Что хранить, а что выкинуть.
Джез выбирает.

---

## Данные
- `data/163/ops.txt` — `PUT <ключ> <значение>` / `GET <ключ>`

## Часть 1
Для каждого GET: `<ключ>: HIT <значение>` или `<ключ>: MISS`.

## Часть 2
Верни `CACHE (MRU first): <ключ>=<значение> ...` — от самого свежего к самому старому.
