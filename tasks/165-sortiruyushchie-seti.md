# 165 — Сортирующие сети

> Уровень: `★★★★★` · Тема: сортирующие сети · компараторы

Сортирующие сети: компараторы, которые учат числа стоять по порядку. Джез собирает схему, в которой
порядок — вопрос геометрии, а не удачи.
— Порядок — это архитектура, — Зен показывает на схему. — Построй правильно — и всё встанет само.
Джез строит порядок.

---

## Данные

- `data/165/net.txt` — компараторы
- `data/165/input.txt` — входная перестановка

## Часть 1

Верни `OUTPUT: <числа после сети>` и `SORTED: YES/NO`.

## Часть 2

Верни `NETWORK LAYERS: <число слоёв>` и `COMPARATORS: 5`.
